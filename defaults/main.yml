---

#replace the user var with role specific one
# replace this with your ansible main_user
tomcat_user: vagrant

tomcat_admin: 'tomcat'
tomcat_admin_pwd: 'tomcat'

tomcat_connector_port: '8081'

# create iptables rule
# if the calling play is going to handle this then set to false
tomcat_firewall: true
